---
fixtures:
  forge_modules:
    stdlib:
      repo: 'puppetlabs/stdlib'
      ref:  '9.6.0'
    concat: 'puppetlabs/concat'
    selinux:
      repo: 'puppet/selinux'
      ref:  '5.0.0'
    systemd:
      repo: 'puppet/systemd'
      ref:  '8.1.0'
    baseapp:
      repo: 'ffquintella/baseapp'
    chocolatey: 'puppetlabs/chocolatey'
  symlinks:
    bastionvault: "#{source_dir}"
